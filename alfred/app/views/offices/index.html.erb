<h1>Lista de oficinas</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th class="text-center">Número de oficina</th>
      <th class="text-center">Edificio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @offices.each do |office| %>
      <tr>
        <td align="center"><%= office.office_number %></td>
        <td align="center"><%= office.department %></td>
        <td align="center"><%= link_to 'Mostrar', office %></td>
        <td align="center"><%= link_to 'Editar', edit_office_path(office) %></td>
        <td align="center"><%= link_to 'Borrar', office, method: :delete, data: { confirm: 'Está seguro de borrar esta oficina?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva oficina', new_office_path %>