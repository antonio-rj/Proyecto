<%= form_for(@office) do |f| %>
  <% if @office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@office.errors.count, "error") %> prohibited this office from being saved:</h2>

      <ul>
      <% @office.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :department, 'Edificio' %><br>
    <%= f.text_field :department %>
  </div>
  <div class="field">
    <%= f.label :office_number, 'Número' %><br>
    <%= f.number_field :office_number %>
  </div>
  
  <div class="actions">
    <%= f.submit "Crear/actualizar oficina" %>
  </div>
<% end %>
